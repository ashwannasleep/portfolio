name = "apple-music-data"
main = "apple-music-data.js"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# Custom domain route for ashwannasleep.com
# Use path routing on main domain
# Note: Routes need to be configured in Cloudflare Dashboard after deployment
# Go to: Workers & Pages > apple-music-data > Triggers > Routes
# Add route: ashwannasleep.com/api/apple-music/*
# For now, routes are commented out - configure in Dashboard instead
# routes = ["ashwannasleep.com/api/apple-music/*"]

[env.production]
vars = { }

